name: Run action

on:
  push:
    paths-ignore:
      - README.md

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Checkout sample
      uses: actions/checkout@v2
      with:
        repository: buildpacks/samples
        path: samples
    - run: ls -l samples
    - name: Run pack
      uses: ./
      with:
        args: '--version'
